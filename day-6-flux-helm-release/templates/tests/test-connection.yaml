apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "day-6-flux-helm-release.fullname" . }}-test-connection"
  labels:
    {{- include "day-6-flux-helm-release.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "day-6-flux-helm-release.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
